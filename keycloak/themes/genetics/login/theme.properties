# genetics login theme: extends the stock keycloak (v2) theme. Only Google/Apple federated
# sign-in is supported, so the username/password form is hidden (css/genetics.css) and the
# social-login label is repurposed as helper text (messages/messages_en.properties).
parent=keycloak
styles=css/styles.css css/genetics.css
