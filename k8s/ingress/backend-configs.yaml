apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: auth-gateway-hc-config
  namespace: genetics
spec:
  healthCheck:
    checkIntervalSec: 15
    timeoutSec: 10
    type: HTTP
    requestPath: /healthz
  timeoutSec: 300
