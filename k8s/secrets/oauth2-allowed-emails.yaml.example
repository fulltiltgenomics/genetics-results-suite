apiVersion: v1
kind: ConfigMap
metadata:
  name: oauth2-allowed-emails
  namespace: genetics
data:
  # add email addresses that should have access (one per line)
  allowed-emails.txt: |
    user@example.com
